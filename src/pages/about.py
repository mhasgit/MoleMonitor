"""About page. Team will add content."""

import streamlit as st


def render() -> None:
    st.title("About MoleMonitor")
    st.info("MVP for tracking skin images. Not a diagnostic tool. Team will add content.")
