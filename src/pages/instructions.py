"""Instructions page. Team will add content."""

import streamlit as st


def render() -> None:
    st.title("Instructions")
    st.info("Guidance for taking consistent photos (distance, lighting, focus, angle). Team will add content.")
