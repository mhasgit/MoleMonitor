# MoleMonitor (Sprint 1)

MVP app for tracking skin images over time. UI scaffolding only.

## Setup

Python 3.10+. Create a venv, then:

```bash
pip install -r requirements.txt
```

## Run

```bash
streamlit run app.py
```

Use the sidebar to switch between Home, Instructions, Image History, and About.
